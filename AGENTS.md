# 项目执行约束

所有协作默认使用简体中文，并以用户明确的优先级为最高执行依据。

## 交付优先

1. 用户要求修复、实现或修改时，先直接完成核心代码与必要配置；不要先写长计划、调研报告或重复解释。
2. 仅在用户明确要求时才输出计划、方案、复盘、审查报告或验收清单。
3. 只改与当前需求直接相关的文件；不得顺带重构、升级依赖、清理无关代码或扩大范围。
4. 遇到可安全作出的工程判断时直接执行；只有权限、数据删除、不可逆操作或需求冲突时才询问用户。

## 验证纪律

1. 默认不运行全量验证、构建或反复启动服务。
2. 只有用户明确要求验证，或当前改动无法以静态检查确认正确性时，才运行一次最小、最相关的验证命令。
3. 同一改动不得因“确认一下”“再检查一次”重复运行测试、构建或启动命令；首次结果已足够时立即停止。
4. 验证失败时只定位与本次改动直接相关的原因并修复；不得转为大范围扫描或额外测试矩阵。
5. 未执行的验证必须如实说明，不得声称已验收、已通过或已在页面确认。

## 工作方式

1. 不自动创建子代理、并行审查或复审；仅在用户明确要求，或任务确实无法由单一直接修改完成时使用。
2. 工具调用前后只给出必要的简短进度，不发送过程性长篇说明。
3. 不反复搜索同一目录、读取无关文件或输出大段命令日志；优先读取目标文件和精确匹配内容。
4. 用户要求“只给计划”时，只写计划，不修改代码或配置。用户要求“完成修复”时，直接实施，不要求重复确认。

## 安全与配置

1. 密码、Token 和密钥默认只写入本地忽略文件或环境变量；只有用户明确要求写入版本控制文件时才允许这样做，并注明影响范围。
2. 不执行删除数据库、重置数据、批量删除文件、覆盖用户未提交改动等操作，除非用户明确授权并确认目标。

## 完成回复

完成回复只包含：已修改的核心内容、必要的使用方式，以及未执行或失败的最小验证。避免逐步过程、重复列表和与请求无关的建议。
